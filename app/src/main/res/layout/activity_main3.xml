<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity3">

    <EditText
        android:id="@+id/etEncryptedPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Encrypted Password"
        android:inputType="text"/>

    <EditText
        android:id="@+id/etDecryptionKey"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/etEncryptedPassword"
        android:hint="Enter Decryption Key"
        android:inputType="number"/>

    <Button
        android:id="@+id/btnDecrypt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/etDecryptionKey"
        android:layout_marginTop="16dp"
        android:text="Decrypt"/>

    <TextView
        android:id="@+id/tvOriginalPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnDecrypt"
        android:layout_marginTop="16dp"/>

</RelativeLayout>
